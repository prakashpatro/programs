string solution(string address) {
int n, i, k=0;
string s="";
n=address.size();

for(i=0; i<n; i++)
{
    if(k==1)
    {
        s=s+address[i];
    }
    if(address[i]=='@' && k==1)
    s="";
    
    if(address[i]=='@')
    k=1;
    
}
return s;
}
