string solution(string code) {
int n, i, j, num, k, t;
string s;
n=code.size();
for(i=0; i<n; i=i+8)
{
    num=0;
    k=128;
    for(j=i; j<i+8; j++)
    {
        t= code[j]-'0';
        num= num + t*k;
        k=k/2;
    }
    s=s+(char)num;
}
return s;
}
