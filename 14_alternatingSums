vector<int> solution(vector<int> a) {
int s1=0, s2=0, i, n;
vector<int> arr;
n=a.size();
for(i=0; i<n; i++)
{
    if(i%2==0)
    {
        s1=s1+a[i];
    }
    else 
    {
        s2=s2+a[i];
    }
}
arr.push_back(s1);
arr.push_back(s2);
return arr;

}
